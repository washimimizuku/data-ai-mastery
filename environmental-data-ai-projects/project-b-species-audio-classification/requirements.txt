# Species Audio Classification Requirements

# Core dependencies
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.5.0
plotly>=5.0.0
jupyter>=1.0.0

# Audio processing
librosa>=0.9.0
soundfile>=0.10.0
audioread>=2.1.0
resampy>=0.3.0

# Machine learning
torch>=2.10.0
torchaudio>=0.12.0
transformers>=4.36.0
scikit-learn>=1.1.0
scipy>=1.8.0

# Hugging Face ecosystem
datasets>=2.0.0
accelerate>=0.20.0

# Audio features and analysis
python_speech_features>=0.6.0
pyaudio>=0.2.11  # For real-time audio capture
wave>=0.0.2

# Web interface
streamlit>=1.20.0
streamlit-audio-recorder>=0.0.8

# Data download and APIs
requests>=2.28.0
urllib3>=2.6.3

# Visualization
seaborn>=0.11.0
plotly>=5.0.0

# Configuration and utilities
pyyaml>=6.0
tqdm>=4.64.0
python-dotenv>=0.19.0

# Testing
pytest>=7.0.0
pytest-cov>=3.0.0

# Optional: For advanced models
# whisper>=1.0.0          # OpenAI Whisper for speech recognition
# speechbrain>=0.5.0      # Advanced speech processing toolkit
pillow>=10.0.1 # not directly required, pinned by Snyk to avoid a vulnerability
pyarrow>=14.0.1 # not directly required, pinned by Snyk to avoid a vulnerability
aiohttp>=3.13.3 # not directly required, pinned by Snyk to avoid a vulnerability
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability
protobuf>=4.25.8 # not directly required, pinned by Snyk to avoid a vulnerability
nbconvert>=7.17.0 # not directly required, pinned by Snyk to avoid a vulnerability
