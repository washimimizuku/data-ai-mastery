# Carbon Footprint Calculator with LLM Requirements

# Core dependencies
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.5.0
plotly>=5.0.0
jupyter>=1.0.0

# LLM and AI frameworks
langchain>=0.0.300
langchain-community>=0.0.20
ollama>=0.1.0
openai>=1.0.0  # Optional: for OpenAI models

# Web frameworks
fastapi>=0.100.0
uvicorn>=0.23.0
streamlit>=1.20.0
pydantic>=2.0.0

# Database
sqlalchemy>=2.0.0
sqlite3  # Built-in Python module
psycopg2-binary>=2.9.0  # For PostgreSQL

# Image processing and OCR
pillow>=10.0.1
opencv-python>=4.5.0
pytesseract>=0.3.10
easyocr>=1.7.0

# PDF processing
pypdf2>=3.0.0
reportlab>=4.0.0

# HTTP requests and APIs
requests>=2.28.0
httpx>=0.24.0

# Data validation and serialization
marshmallow>=3.26.2
cerberus>=1.3.0

# Configuration and utilities
pyyaml>=6.0
python-dotenv>=0.19.0
click>=8.0.0

# Email and notifications
smtplib  # Built-in Python module
twilio>=8.0.0  # For SMS notifications

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=3.0.0

# Optional: Advanced features
# transformers>=4.20.0     # For fine-tuning models
# torch>=1.12.0           # For local model training
# sentence-transformers>=2.2.0  # For embeddings
starlette>=0.49.1 # not directly required, pinned by Snyk to avoid a vulnerability
aiohttp>=3.13.3 # not directly required, pinned by Snyk to avoid a vulnerability
torch>=2.9.0 # not directly required, pinned by Snyk to avoid a vulnerability
urllib3>=2.6.3 # not directly required, pinned by Snyk to avoid a vulnerability
