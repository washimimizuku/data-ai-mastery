# Environmental Sensor Data Streaming Requirements

# Core dependencies
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.5.0
plotly>=5.0.0
jupyter>=1.0.0

# Streaming and messaging
kafka-python>=2.0.0
confluent-kafka>=2.0.0
paho-mqtt>=1.6.0

# Time series database
influxdb-client>=1.36.0
influxdb>=5.3.0

# Real-time processing
apache-beam>=2.40.0  # For advanced stream processing
streamz>=0.6.0       # Lightweight streaming

# Machine learning for anomaly detection
scikit-learn>=1.1.0
scipy>=1.8.0
isolation-forest>=0.1.0

# Web interface and dashboards
streamlit>=1.20.0
dash>=2.6.0
bokeh>=3.8.2

# Data serialization
avro-python3>=1.10.0
protobuf>=6.33.5

# Configuration and utilities
pyyaml>=6.0
python-dotenv>=0.19.0
schedule>=1.2.0  # For periodic tasks

# Monitoring and logging
prometheus-client>=0.14.0
structlog>=22.0.0

# Containerization support
docker>=6.0.0
docker-compose>=1.29.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=3.0.0

# Optional: Advanced features
# kubernetes>=24.0.0       # For K8s deployment
# redis>=4.3.0            # For caching and pub/sub
# celery>=5.2.0           # For background tasks
fonttools>=4.61.0 # not directly required, pinned by Snyk to avoid a vulnerability
pillow>=10.0.1 # not directly required, pinned by Snyk to avoid a vulnerability
urllib3>=2.6.3 # not directly required, pinned by Snyk to avoid a vulnerability
pyarrow>=14.0.1 # not directly required, pinned by Snyk to avoid a vulnerability
nbconvert>=7.17.0 # not directly required, pinned by Snyk to avoid a vulnerability
