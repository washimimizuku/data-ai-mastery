# Camera Trap AI Requirements

# Core dependencies
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.5.0
plotly>=5.0.0
jupyter>=1.0.0

# Computer vision and deep learning
torch>=2.10.0
torchvision>=0.13.0
opencv-python>=4.5.0
pillow>=10.0.1

# Object detection
ultralytics>=8.0.0  # YOLOv8
detectron2>=0.6.0   # Facebook's detection framework

# Image processing and augmentation
albumentations>=1.3.0
imgaug>=0.4.0

# Dataset management
roboflow>=1.0.0
supervision>=0.1.0  # Computer vision utilities

# Model deployment
onnx>=1.16.2
onnxruntime>=1.12.0

# Web interface
streamlit>=1.20.0
streamlit-webrtc>=0.45.0  # Real-time video processing

# Data visualization
seaborn>=0.11.0

# Configuration and utilities
pyyaml>=6.0
python-dotenv>=0.19.0
tqdm>=4.64.0

# Testing
pytest>=7.0.0
pytest-cov>=3.0.0

# Optional: Advanced features
# tensorflow>=2.9.0        # Alternative to PyTorch
# megadetector>=5.0.0      # Microsoft's wildlife detector
# timm>=0.6.0              # PyTorch image models
nbconvert>=7.17.0 # not directly required, pinned by Snyk to avoid a vulnerability
protobuf>=6.33.5 # not directly required, pinned by Snyk to avoid a vulnerability
jupyter-core>=5.8.0 # not directly required, pinned by Snyk to avoid a vulnerability
tornado>=6.5.3 # not directly required, pinned by Snyk to avoid a vulnerability
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability
