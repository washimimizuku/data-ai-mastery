# Biodiversity Threat Assessment NLP Requirements

# Core dependencies
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.5.0
plotly>=5.0.0
jupyter>=1.0.0

# Natural language processing
spacy>=3.4.0
transformers>=5.0.0rc1
datasets>=2.0.0
tokenizers>=0.13.0

# LLM frameworks
langchain>=0.0.300
langchain-community>=0.0.20

# Text processing and analysis
nltk>=3.8.0
textblob>=0.17.0
scikit-learn>=1.1.0

# Named entity recognition
spacy-transformers>=1.2.0
scispacy>=0.5.0  # Scientific text processing

# Network analysis
networkx>=2.8.0

# Web scraping and APIs
requests>=2.28.0
beautifulsoup4>=4.11.0
scrapy>=2.14.0
newspaper3k>=0.2.8

# Scientific literature APIs
biopython>=1.79  # For PubMed access
scholarly>=1.7.0 # Google Scholar API

# Web interface
streamlit>=1.20.0

# Data visualization
seaborn>=0.11.0
wordcloud>=1.9.0

# Configuration and utilities
pyyaml>=6.0
python-dotenv>=0.19.0
tqdm>=4.64.0

# Testing
pytest>=7.0.0
pytest-cov>=3.0.0

# Optional: Advanced NLP features
# torch>=1.12.0            # For custom model training
# sentence-transformers>=2.2.0  # Semantic similarity
# gensim>=4.2.0            # Topic modeling
pillow>=10.0.1 # not directly required, pinned by Snyk to avoid a vulnerability
pyarrow>=14.0.1 # not directly required, pinned by Snyk to avoid a vulnerability
aiohttp>=3.13.3 # not directly required, pinned by Snyk to avoid a vulnerability
filelock>=3.20.3 # not directly required, pinned by Snyk to avoid a vulnerability
torch>=2.9.0 # not directly required, pinned by Snyk to avoid a vulnerability
urllib3>=2.6.3 # not directly required, pinned by Snyk to avoid a vulnerability
nbconvert>=7.17.0 # not directly required, pinned by Snyk to avoid a vulnerability
protobuf>=6.33.5 # not directly required, pinned by Snyk to avoid a vulnerability
